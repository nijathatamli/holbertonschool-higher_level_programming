📌 Python Abstract Base Classes (ABC)
🧩 What is an Abstract Base Class?

In Python, an Abstract Base Class (ABC) is a class that cannot be instantiated and is used to define a common interface for its subclasses.

ABC ensures:

All subclasses implement required methods

Code structure is consistent

Polymorphism is possible

Large projects stay organized and scalable

Python provides ABC support via the abc module:
